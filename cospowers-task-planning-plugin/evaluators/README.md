# Evaluators

This split plugin ships evaluator skills under `skills/` so they can be used without depending on another plugin package.

- `skills/plan-evaluator/` evaluates implementation plans produced by `writing-plans` before downstream handoff.
