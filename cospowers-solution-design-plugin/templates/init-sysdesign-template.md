# 总系统设计阶段 · 背景情报简报

> **🤖 AI 使用说明**
>
> 本文档由 AI 在开始生成总系统设计前发送给用户。
> 目的是收集 AI **无法从系统需求文档和代码库中自行推断**的架构约束和团队决策。
>
> **填写规则：自由格式即可。**
> 重点提供：已有架构图、历史设计文档、团队已定下的技术选型、不能动的现有服务。
> AI 会基于这些约束输入提出 2-3 个架构方案让你选择，而不是凭空设计。
>
> **只需要填你知道的部分，不知道的跳过。**
> AI 校验的唯一标准是：§1 现有架构图 或 §2 历史设计文档 至少有一项有内容。

---

## §1 现有架构图 ⭐强烈建议提供

> **这是最有价值的输入。** AI 看到架构图后可以：
> - 准确识别哪些模块已经存在，避免重复设计
> - 定位到知识库中对应模块的接口和数据结构
> - 理解现有服务的边界，设计更准确的集成方案
>
> **支持格式（任选其一即可）：**
> - draw.io 文件路径（.drawio 或 .xml）
> - Mermaid/PlantUML 代码（直接粘贴）
> - **文字描述**：列出各服务名称及调用/依赖关系，AI 会据此生成架构图
>
> **文字描述示例：**
> ```
> 服务列表及在本项目中承担的功能：
> - API Gateway：本项目所有请求的统一入口，负责路由分发和鉴权
> - User Service：承担本项目的用户模块，处理注册、登录和权限校验
> - Order Service：承担本项目的订单模块，覆盖从下单到履约的完整流程
> - Payment Service：承担本项目的支付模块，处理扣款、退款和对账
> - MySQL：存储本项目的订单和用户数据
> - Redis：为用户模块提供登录态缓存
>
> 调用关系：
> - 客户端 → API Gateway（HTTP）
> - API Gateway → User Service / Order Service（gRPC）
> - Order Service → Payment Service（gRPC）
> - Order Service → MySQL（读写），User Service → Redis（缓存）
> ```

*(在这里贴文件路径、代码，或用文字描述服务关系...)*

---

## §2 依赖文档 （选填）

> 提供 AI 设计时需要参考的文档，不限于历史版本——任何对本次设计有约束或参考价值的文档均可。
> AI 会从中提取已确定的架构决策、接口定义、数据结构，在本次设计中保持一致或明确说明变更原因。

- *(例：上一版总体设计 `docs/specs/2025-06-01-quota-system-design.md`)*
- *(例：相关 ADR 决策记录 `docs/adr/adr-0012-use-rocketmq.md`)*
- *(例：依赖服务的接口文档 `docs/api/auth-service-openapi.yaml`)*
- *(例：需求文档 `docs/requirements/2025-Q3-quota-feature.md`)*
- *(例：技术方案评审文档等)*

---

## §3 已确定的技术选型和约束 （选填）

> 团队已经做出的技术决策，或者有明确约束不能更改的选项。
> AI 会把这些作为硬约束，在方案中不重新讨论。
>
> **只写已经决定的，还没决定的不用写（AI 会提方案）。**

- *(例：消息队列只能用 RocketMQ，不能引入 Kafka)*
- *(例：所有新服务必须跑在 K8s 上)*
- *(例：数据库只能用 MySQL，不能用 PostgreSQL)*
- *(例：鉴权必须走现有的 auth-service，不能绕过)*
- *(例：这次不能加新的微服务，只能在现有服务中扩展)*

---

## §4 必须复用或不能改动的现有服务 （选填）

> 有哪些现有的服务、模块、或数据库表，是这次设计必须复用或不能改动的？
> AI 会在设计时把这些作为既成事实，只在它们的边界之外设计新内容。

- *(例：用户认证必须复用 auth-service 的 /token/validate 接口)*
- *(例：tenant 表和 user 表的结构不能加字段)*
- *(例：通知功能必须复用现有的 notification-service)*
- *(例：资源计量数据只能从 skyops-agent 上报的 metrics 读取)*

---

## §5 与外部系统的集成关系 （选填）

> 这个系统需要与哪些外部系统或第三方平台集成？
> 包括：需要调用的外部 API、需要接收数据的上游系统、需要推送数据的下游系统。

- *(例：需要接收 SkyOps 推送的 VM 资源变更事件)*
- *(例：配额数据需要同步给 DMP 做用量分析)*
- *(例：审批流程需要发通知到企业微信)*

---

## §6 已知的性能和规模约束 （选填）

> 有没有已知的具体数字约束？比如：用户规模、数据量、并发峰值。
> 不用猜测，只写你确定的。没有具体数字可以不填。

*(在这里写...)*

---

## §7 其他设计约束或背景 （选填）

> 任何影响架构决策的背景信息，比如：
> 历史遗留问题、正在进行的其他重构、合规要求、团队技术能力约束等。

*(在这里写...)*

---

## §8 涉及服务的本地代码路径 ⭐强烈建议提供

> AI 在进行系统设计前需要读取现有代码来：
> - 确认现有接口签名和数据结构（避免设计与已有实现不一致）
> - 理解现有模块的边界和可复用点
> - 识别技术债务和需要避让的区域
>
> 请提供每个涉及服务的**本地代码根目录**路径（绝对路径）。
> AI 生成架构方案前会先扫描这些目录，基于真实代码提出设计，而不是凭空推断。

| 服务/子系统名 | 本地代码根目录路径 | 说明（可选） |
|---|---|---|
| *(例：user-service)* | *(例：D:\code\user-service)* | *(例：用户认证模块)* |
| *(例：order-service)* | *(例：D:\code\order-service)* | |

*(如涉及服务较多，直接列文本行也可以)*
