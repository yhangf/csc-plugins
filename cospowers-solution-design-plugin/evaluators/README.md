# Evaluators

Evaluator skills are exposed through `skills/*-evaluator/` for current platform compatibility. This directory is an index for humans and future evaluator-specific loaders.

- `skills/sysdesign-evaluator/`
- `skills/subsystem-evaluator/`
- `skills/doc-quality-evaluator/`
