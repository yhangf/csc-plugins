# 系统需求阶段 · 背景情报简报

> **🤖 AI 使用说明**
>
> 本文档由 AI 在启动系统需求分析前发送给用户。
> 目的是收集 AI **无法从代码库和现有文档中自行推断**的背景知识。
>
> **填写规则：自由格式即可，不需要按照任何规范格式。**
> 写几句话、粘贴旧文档片段都可以。
> AI 会基于这些情报完成格式化工作。
>
> **只需要填你知道的部分，不知道的跳过。**
> AI 校验的唯一标准是：§1 业务背景 和 §2 关键场景 有内容即可继续。

---

## §1 业务背景 ⭐必填

> 这次做这个功能/迭代，背后的业务原因是什么？为谁解决什么问题？
> 有什么历史背景或者上下文需要 AI 了解？
>
> **自由写，不限格式。**

*(在这里写...)*

---

## §2 关键业务场景 ⭐必填

> 用户（或系统）在什么情况下会用到这个功能？完成后期望得到什么？
> 想到几个写几个，用自己的语言描述即可，不需要 Given/When/Then 格式。
>
> **举例：** "运维在月底需要统一给各租户调整资源配额，现在要手动一个个改，很麻烦，希望能批量操作"

*(在这里写...)*

---

## §3 关联的历史版本文档 （选填）

> 有没有之前版本的需求文档、PRD、设计文档、或者会议纪要？
> 贴文件路径即可。AI 会读取并做增量对比，避免重复定义已有需求。

- *(例：上一版系统需求文档 `docs/specs/2025-09-01-quota-system-requirements.md`)*
- *(例：上一次评审会的会议纪要 `docs/meeting/2025-11-review.md`)*

---

## §4 涉及的关联服务和系统 （选填）

> 这个功能会调用哪些其他服务？会被哪些系统调用？有哪些必须对接的外部系统？
> AI 会用这些名字去查知识库，理解接口和数据结构。
>
> **只需要写服务/模块的名字或描述，不需要写接口细节。**

- *(例：需要调用云代理服务的资源上报接口)*
- *(例：前端控制台会调用这个功能的 BFF 接口)*
- *(例：通知要走现有的消息通知服务)*
- *(例：数据需要同步到数据管理平台)*

---

## §5 现有架构图或系统全景图 （选填，强烈建议提供）

> 有没有现有的架构图、系统全景图、组件关系图？
> AI 看到架构图可以准确理解各模块的位置关系，从知识库中找到对应模块的详细信息，避免理解偏差。
>
> **支持格式：**
> - draw.io 文件路径（.drawio 或 .xml）
> - Mermaid/PlantUML 代码（直接粘贴）

*(在这里贴图片路径或文件路径...)*

---

## §6 领域术语和业务规则 （选填）

> 有没有这个业务领域特有的术语、缩写、或者业务规则，是 AI 从代码里不容易看出来的？
> 例如：特定的业务状态机、计费规则、合规要求、行业惯例等。

*(在这里写...)*

---

## §7 其他 AI 不知道的上下文 （选填）

> 任何你认为对 AI 理解这次需求有帮助的信息，都可以在这里补充。
> 比如：这个需求背后的政治因素、历史遗留决策、未来方向等。

*(在这里写...)*

---

## §8 版本需求范围界定 （选填，强烈建议提供）

> **这次迭代要做到什么程度？需求内容可能分多个版本迭代实现。**
> 明确写出本版本的边界，帮助 AI 准确区分"本期需求"与"后续迭代"，避免过度设计或遗漏核心功能。

**本版本包含：**
- *(例：用户可以创建/编辑/删除配额策略，审批流程可以发起和驳回)*
- *(例：支持租户级别的配额查看和申请)*

**本版本明确不包含（留给后续版本）：**
- *(例：配额使用率的图表展示，留 v2 实现)*
- *(例：自动化配额调整策略，留后续迭代)*
- *(例：多级审批流程，本期只做单级审批)*

**后续版本规划（可选）：**
- *(例：v2 补充图表展示和告警通知)*
- *(例：v3 引入 AI 推荐配额策略)*

---

## §9 DFX 约束背景（由 AI 预填，请人工确认修改）

> **说明：** AI 在写入本简报前会查询团队知识库（如可用）获取平台适用的 DFX 规范基线，
> 并将查询结果预填到下方。请确认或修改这些约束，确保它们符合本次版本的实际情况。
>
> **如果知识库未返回内容，请手动填写关键 DFX 约束，AI 会把这些作为硬约束写入系统需求文档的 §4 DFX 章节。**

**性能基线（来自知识库，请确认）：**
*(AI 预填区域 — 查询关键词：性能指标 / 响应时间基线)*

**可靠性基线（来自知识库，请确认）：**
*(AI 预填区域 — 查询关键词：可用性要求 / 故障恢复)*

**可监控性约束（来自知识库，请确认）：**
*(AI 预填区域 — 查询关键词：业务监控 / 监控指标规范)*

**安全约束（来自知识库，请确认）：**
*(AI 预填区域 — 查询关键词：安全基线 / 鉴权规范)*

---

## §10 结构化补充信息 ⭐强烈建议提供（如有前端界面或明确业务规则）

> **AI 利用这 4 类信息可以直接生成更准确的 REQ 条目和验收条件，避免反复澄清。**
> 每项写你知道的部分即可，不知道跳过。
>
> **如全部不涉及，请填写：** 不涉及

---

### §10.1 数据模型字段需求

> 这个功能涉及哪些实体/表？每个实体有哪些字段？AI 会据此生成字段级 REQ 条目。
>
> **自由格式即可**，可以是表格、列表或粗略描述：
> - 字段名称（中英文均可）
> - 字段类型或取值范围（可选）
> - 是否必填、唯一、只读（可选）
>
> **举例：** "配额策略表：策略名称（必填，最长64字符）、资源类型（枚举：CPU/内存/存储）、配额上限（数字，>=0）、生效时间（时间戳）、状态（草稿/生效/失效）"

*(在这里写...)*

---

### §10.2 API 接口推导

> 这个功能需要哪些对外接口？哪些页面/操作会触发后端调用？AI 会据此补充接口级验收条件。
>
> **举例：**
> - "配额策略列表页 → 分页查询接口（支持按资源类型过滤）"
> - "新建弹窗提交 → 创建策略接口（返回策略ID）"
> - "删除按钮 → 软删除接口（需检查是否被引用）"
> - "详情页编辑后保存 → 更新接口（仅允许草稿状态可编辑）"

*(在这里写...)*

---

### §10.3 枚举值与状态机

> 哪些字段有固定取值？有没有需要管理的业务状态流转（状态机）？AI 会生成对应的枚举约束和状态转换 REQ。
>
> **举例（枚举值）：** "资源类型：CPU、内存、GPU、存储（四选一）"
> **举例（状态机）：** "策略状态：草稿 → 审批中 → 生效；生效 → 失效；草稿可直接删除；生效状态不可编辑"

*(在这里写...)*

---

### §10.4 业务规则

> 有没有需要在后端强制执行的校验规则、计算逻辑或权限控制？AI 会将这些规则转化为可测试的异常场景 REQ。
>
> **举例（校验）：** "配额上限不能低于当前已分配量"
> **举例（计算）：** "实际可用配额 = 配额上限 - 已分配量，实时计算，不存储"
> **举例（权限）：** "只有资源管理员角色可以创建和删除策略；租户管理员只能查看"
> **举例（联动）：** "策略生效后，关联租户的可用配额上限自动更新"

*(在这里写...)*
